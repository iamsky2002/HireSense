package com.sky.hiresense.job;

// Employment types used in frontend dropdowns and job filtering.
public enum EmploymentType {
    FULL_TIME,
    PART_TIME,
    CONTRACT,
    INTERNSHIP,
    TEMPORARY
}
